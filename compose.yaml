services:
  botcauciones:
    build: .
    container_name: botcauciones
    restart: unless-stopped
    env_file:
      - .env
    # Red host para acceder a PostgreSQL en otro servidor
    network_mode: host
    # Alternativamente, si usas red bridge:
    # networks:
    #   - default
    # extra_hosts:
    #   - "leningrad:192.168.x.x"  # IP de tu servidor PostgreSQL
